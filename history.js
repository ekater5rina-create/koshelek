/* В опубликованной версии встроенной истории нет: финансовые данные не выкладываются
   в открытый доступ. Свои операции загружайте через «Ещё» → «Восстановить из копии»
   или «Импорт файла „Планировщик бюджета“» — они останутся только на вашем телефоне. */

const HISTORY = {};

function buildHistory() {
  return { transactions: [], report: [] };
}
